import { Text, View, ScrollView } from 'react-native';

function HomeScreen() {
    return (
        <ScrollView>
            <View>
                <Text>This is Home Screen</Text>
            </View>
        </ScrollView>
    );
}

export default HomeScreen;
