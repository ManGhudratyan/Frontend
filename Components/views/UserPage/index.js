import { Text, View, ScrollView } from 'react-native';

function UserPage() {
    return (
        <ScrollView>
            <View>
                <Text>This is User Page</Text>
            </View>
        </ScrollView>
    );
}

export default UserPage;
